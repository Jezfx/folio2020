import React from "react";

import * as Styles from "./footer.styles";

const Footer: React.FunctionComponent = (): JSX.Element => {
  return <Styles.Container as="footer" />;
};

export default Footer;
